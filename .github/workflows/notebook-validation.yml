name: Notebook Validation

on:
  push:
    paths:
      - "bolumler/b*/calisma.ipynb"
      - "requirements.txt"
      - "tools/dogrula_notebooklar.py"
      - "tools/calistir_notebooklar.py"
      - ".github/workflows/notebook-validation.yml"
  pull_request:
    paths:
      - "bolumler/b*/calisma.ipynb"
      - "requirements.txt"
      - "tools/dogrula_notebooklar.py"
      - "tools/calistir_notebooklar.py"
      - ".github/workflows/notebook-validation.yml"
  workflow_dispatch:

jobs:
  validate-notebooks:
    runs-on: ubuntu-latest
    steps:
      - name: Depoyu al
        uses: actions/checkout@v4

      - name: Python kur
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Statik notebook doğrulaması
        run: python tools/dogrula_notebooklar.py

      - name: Notebook çalışma ortamını kur
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt nbformat nbclient ipykernel

      - name: On altı notebooku çalıştır
        run: python tools/calistir_notebooklar.py
